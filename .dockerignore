node_modules
npm-debug.log
.git
.gitignore
# Claude Code's worktrees live under .claude/ and each one carries its own node_modules, so
# without this the build context grows by a whole second checkout.
.claude
.env
.env.*
dist
# Regenerated by `vite build`; copying a stale one in only risks confusing the build.
.svelte-kit
data
*.log
.DS_Store
.vscode
.idea
*.swp
*.swo
README.md
CLAUDE.md
docs
.dockerignore
Dockerfile
docker-compose.yml
.github
.gitlab-ci.yml
.travis.yml